import { View } from "react-native"
import { Text } from "react-native-paper"

const HomeScreen = () => {
    return (
        <View style={{ flex: 1, width: '100%', alignItems: 'center', justifyContent: 'center', gap: 5 }} >
            <Text>Home Scrreen</Text>
        </View>
    )
}

export default HomeScreen